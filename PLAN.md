# 词爆 WordPop — 项目计划

> macOS 单词弹出打卡应用 · 从 0 到 1 的完整交付计划
> 目标机器：MacBook Pro（Apple Silicon / M 芯片，macOS 26.5）
> 技术栈：原生 Swift + SwiftUI / AppKit（SwiftPM 构建，无第三方依赖）

## 0. 项目定位（一句话）

一款常驻菜单栏的 macOS 原生小工具：按你设定的节奏，把一批英文单词像泡泡一样随机「喷」到屏幕上；**hover 显示释义，双击点爆消失**，鼠标穿透不打扰工作——把背单词变成零负担的「随手一击」。

## 1. 总体阶段与交付物

| 阶段 | 内容 | 交付物 | 状态 |
|---|---|---|---|
| 0 | 环境确认 | M 芯片 / Swift 6.3 / macOS SDK / WindowServer 可用 | ✅ 已完成 |
| 1 | 需求与产品定义 | `PLAN.md`、`PRD.md` | ⏳ 本次 |
| 2 | 视觉与交互设计 | `DESIGN.md`、`docs/mockup.html`（高保真稿） | 下一步 |
| 3 | 工程搭建 | `Package.swift`、打包/图标脚本、.app 装配 | 下一步 |
| 4 | 功能实现 | SwiftUI + AppKit 源码（词库/悬浮层/设置/统计） | 下一步 |
| 5 | 构建与自测 | `swift build` 通过 → 打包 → 启动冒烟 → 截图验证 | 下一步 |
| 6 | 交付 | `README.md`、使用说明、路线图 | 最后 |

## 2. 关键产品决策（已与用户确认）

1. **词泡内容**：只显示英文单词；**鼠标 hover 后** 才展示中文释义（先回忆，后确认）。
2. **悬浮方式**：单词悬浮层**不拦截鼠标**，可继续手头工作，随手双击清除。
3. **词库**：**内置分级精选词库**（开箱即用）+ 支持导入自定义 TXT/CSV 词本。
4. **界面语言**：中文为主，单词内容为英文。

## 3. 里程碑拆解

### M1 产品文档（阶段 1–2）
- PRD：功能优先级 P0/P1/P2、验收标准、边界情况、v0.1–v1.0 路线。
- 设计：玻璃拟态 + 柔和渐变 + 弹簧动效的设计语言；词泡 / 释义卡 / 菜单栏 / 设置窗口组件规范；高保真 UI 稿。

### M2 可跑通的核心闭环（阶段 3–5）
- 菜单栏常驻（`MenuBarExtra`），Accessory 模式（无 Dock 图标）。
- 定时引擎：N 分钟弹出 1 波，波次可手动触发。
- 悬浮引擎：全屏幕随机、互不重叠地生成玻璃词泡；多屏支持；鼠标穿透空区域。
- 交互闭环：hover 出释义卡（可发音）→ 双击点爆（粒子动画）→ 记录「今日点掉」。
- 词库：内置 2 级精选词表 + 导入解析 + 本地 JSON 持久化。
- 设置窗口：频率 / 波量 / 留存时长 / 词库 / 外观主题 / 统计。
- 打包：生成 `词爆.app`（含图标、Info.plist、LSUIElement、广告签名）。

### M3 打磨（视剩余资源）
- 屏幕截图验证、动效调参、README、路线图。

## 4. 质量与验收红线

- `swift build -c release` 零错误；打包后可 `open 词爆.app` 启动，进程存活。
- 悬浮词泡不抢键盘焦点、不拦截空白区域点击；双击准确点爆；间隔可配置并即时生效。
- 词库导入支持 UTF-8 TXT/CSV（兼容 BOM），重复词条去重，坏行跳过并提示。
- UI 遵守设计规范（圆角、留白、字阶、动效曲线），中文文案统一。

## 5. 风险与对策

| 风险 | 对策 |
|---|---|
| 词泡窗口遮挡全屏 App 时的层级 | 使用 `.canJoinAllSpaces + fullScreenAuxiliary` 的 NSPanel；提供「置顶级别」设置项 |
| hover 事件依赖鼠标移入小窗口 | 词泡窗口即词泡热区，AppKit Tracking + SwiftUI onHover 双保险 |
| 多窗口动效耗电 | 词泡为轻量无阴影透明 NSPanel + 低帧率摆浮动效；退出波次即销毁窗口 |
| 内置词库容量有限 | v1 提供精选 200+ 词，开放导入；路线图规划四级/六级/考研/雅思完整包 |
| 无完整 Xcode（仅 CLT） | 用 SwiftPM 工程 + 自写打包脚本装配 .app，Xcode 后续可直接打开 |

## 6. 版本规划

- **v0.1（本交付）**：核心闭环 + 设置 + 词库导入 + 基础统计 + 打包运行。
- **v0.2**：学习记忆曲线（点爆后间隔复现）、遗忘词重投、发音按钮热键、快捷键暂停。
- **v1.0**：完整分级词库下载、词义词典服务、多用户词本云同步（如需要）、小组件。

---
*计划文档结束。详细需求见 `PRD.md`，视觉规范见 `DESIGN.md`。*
