# 词爆 WordPop — 产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 产品名 | 词爆 WordPop（英文名 WordPop） |
| 形态 | macOS 原生菜单栏常驻应用（Accessory，无 Dock 图标） |
| 目标平台 | macOS 14+，Apple Silicon / Intel 均兼容（本机为 M 芯片） |
| 技术 | SwiftUI + AppKit，SwiftPM 构建，无第三方依赖 |
| 文档版本 | v0.1-draft |
| 状态 | 已与用户确认核心形态后定稿 |

---

## 1. 背景与问题

背单词最大的敌人是「没打开 App / 没到复习时间 / 不想正襟危坐地背」。现有方案要么是通知推送（看完即忘、可跳过），要么是打断式强制学习（体验重）。

**词爆**把复习变成一个「随手就能完成」的轻游戏：单词定时出现在你正在用的屏幕上，不抢焦点、不打断输入；你想起来就 hover 看一眼释义、双击点爆；不想理它就自己飘走。用高频低成本的暴露 + 主动回忆，把碎片时间变成单词记忆时间。

## 2. 产品目标

- **P0 目标**：让用户无需打开任何界面，就能持续、被动地接触要复习的单词。
- 每次点爆 = 一次「主动回忆 + 即时反馈」，强化记忆。
- 提供「立即弹一波」的人肉触发，方便随时突击。
- 本地优先：词库、设置、统计全部本地存储，无网络依赖，无隐私风险。

## 3. 用户与场景

- **目标用户**：准备四六级 / 考研 / 雅思托福，或日常积累词汇的 Mac 用户；喜欢「番茄钟/桌面宠物」式轻工具的审美派用户。
- **典型场景**：
  1. 写代码时每 20 分钟弹 6 个词，边敲边双击，一天攒 300+ 次暴露。
  2. 开会摸鱼 / 等编译时手动「立即弹一波」。
  3. 考前 1 个月导入自己的高频错词本，全屏轰炸强记。
- **反场景**：需要沉浸阅读/剪辑时不希望被打扰 → 提供暂停与自动飘走机制。

## 4. 名词表

| 术语 | 定义 |
|---|---|
| 波（Wave） | 一次定时触发的弹出动作，含 N 个词泡 |
| 词泡（Bubble） | 屏幕上单个悬浮的英文单词胶囊 |
| 点爆（Pop） | 对词泡双击，触发粒子动画并使其消失 |
| 释义卡（Tip） | hover 词泡时出现的英文+中文小卡片 |
| 留存时长 | 词泡未被点爆时的最长存在时间，超时自动飘走 |
| 词本（Bank） | 词条的集合：内置分级精选或用户导入 |

## 5. 核心信息架构

```
菜单栏图标（词爆）
 ├─ 状态区：下次弹出倒计时 / 已暂停
 ├─ 立即弹一波
 ├─ 暂停弹出 / 继续弹出
 ├─ 今日已点掉 N 词
 ├─ 设置…
 └─ 退出

设置窗口（760×620）
 ├─ 通用：频率、每波词数、留存时长、残留处理、启动行为、声音/触感
 ├─ 词库：来源选择（内置/我的词本）、导入、预览、删除、格式说明
 ├─ 外观：主题（晨雾/午夜/晚霞）、强调色、字号、字体风格
 ├─ 统计：今日/累计、近 7 日柱状图、词库规模
 └─ 关于：版本与致谢
```

## 6. 功能需求

> 优先级：P0 必须（MVP）/ P1 重要 / P2 打磨。验收标准（AC）逐条可测。

### FR-1 定时引擎（P0）
- 每经过可配置的**弹出间隔**（预设 5/10/15/20/30/45/60 分钟，支持自定义 1–180 分钟）触发一波。
- 间隔修改立即生效（下一波重新计时），无需重启。
- 菜单栏显示「距下一波 mm:ss」实时倒计时；已暂停时显示「已暂停」。
- AC1.1：设 1 分钟，60s±1s 内弹出第一波。AC1.2：修改间隔后按新间隔重排倒计时。

### FR-2 弹出与词泡（P0）
- 每波从当前词本中随机取 **M 个**（默认 6，可设 4–16）不重复单词，且避开最近 3 波出现过的词（池不足时放宽）。
- 词泡随机散布在**所有外接/内建屏幕**的可见区域内，互不重叠（含 16pt 间距），避开菜单栏/Dock。
- 词泡只显示英文单词（气泡胶囊：毛玻璃+渐变描边+柔和阴影）；文本样式受「外观」设置影响。
- 词泡带轻微上下浮动（慢速正弦，幅度 ≤ 4pt），入场为弹簧缩放淡入。
- AC2.1：连续 10 波无重叠碰撞。AC2.2：多屏接入/拔出后仅在有屏区域生成。AC2.3：屏幕可见区（菜单栏/Dock 之外）内完整显示。

### FR-3 hover 释义（P0，用户指定形态）
- 鼠标悬停词泡约 **0.25s** 后，词泡附近弹出释义卡：大字英文 + 中文释义 + 发音按钮。
- 移出词泡约 0.15s 后释义卡淡出；释义卡不遮挡光标下的词泡。
- 释义卡贴近屏幕边缘时自动反向/内收，保证完整可见。
- AC3.1：释义卡出现不改变词泡位置。AC3.2：释义卡可点击发音（系统英文语音）。

### FR-4 双击点爆（P0）
- 对词泡**双击**触发点爆：词泡先放大 12% 再缩散，伴随 14–22 颗彩色粒子向四周迸发并淡出，约 0.9s 后词泡销毁。
- 点爆后计数：今日点掉 +1、累计 +1。
- 可选触感反馈（macOS 触控板 haptic）。
- 单击不做任何破坏性动作（可轻亮反馈）。
- AC4.1：双击后词泡必然在 1.2s 内消失。AC4.2：点爆不触发其下方 App 的点击（词泡窗口不透明部分专属消费）。
- AC4.3：点爆过程不抢键盘焦点、不激活本应用。

### FR-5 自动飘走（P0）
- 词泡超过**留存时长**（默认 3 分钟，可设 1–15 分钟）仍未被点爆，则以「上飘+淡出」动画消失，保证屏幕不被长期占用。
- 新一波开始时，若屏幕上仍有残留词泡：默认先让残留快速飘走（0.4s）再落新词；设置项可改为「允许叠加直到上限 24 个」。
- AC5.1：词泡到期必自动清理，无内存泄漏（可连续运行 24h 验证窗口数归零）。

### FR-6 手动触发与暂停（P0）
- 菜单栏「立即弹一波」：无视计时立刻弹出一波，且不重置周期计时。
- 「暂停/继续」：暂停期间不弹、倒计时冻结；继续后从剩余时间恢复。
- AC6.1：暂停→等待 3 倍间隔不弹；恢复后按剩余时间弹出。

### FR-7 词库管理（P0）
- **内置词库**：开箱即用，按级别分组（v0.1 提供「基础精选」与「进阶精选」两级，约 200+ 词条，均含中文释义）。
- **我的词本**：支持导入 UTF-8 的 `.txt` / `.csv`：
  - 每行一条：`word` 或 `word[Tab/空格]中文释义`（csv 用逗号分列，兼容引号与 BOM）。
  - `#` 开头与空行忽略；重复词自动去重（大小写不敏感）；含非法字符的行跳过并在结果中报告。
  - 导入后本地 JSON 持久化（Application Support/WordPop），可预览、删除单条、清空。
- 词条字段：`id`、`word`（英文原文）、`meaning`（中文释义）。
- AC7.1：导入 1000 行耗时 < 1s 且无重复。AC7.2：重启后词本仍在。AC7.3：当前词本为空时定时引擎不发波（菜单栏提示补词）。

### FR-8 设置持久化（P0）
- 全部设置即改即存（UserDefaults），重启生效。
- 设置项明细：

| 分组 | 设置 | 取值/默认 |
|---|---|---|
| 通用 | 弹出间隔 | 预设 + 自定义 1–180 分钟 / 默认 20 |
| 通用 | 每波词数 | 4–16 / 默认 6 |
| 通用 | 留存时长 | 1–15 分钟 / 默认 3 |
| 通用 | 残留处理 | 先飘走（默认）或叠加至 24 |
| 通用 | 启动时自动开始 | 开 / 默认开 |
| 通用 | 点爆触感 | 开 / 默认开 |
| 词库 | 当前词源 | 内置·基础 / 内置·进阶 / 我的词本 |
| 外观 | 主题 | 晨雾 Glass / 午夜 Glass / 晚霞 Aurora / 默认 晨雾 |
| 外观 | 强调色 | 靛蓝/青/粉/紫/薄荷/橙 8 色 / 默认 靛蓝 |
| 外观 | 词泡字号 | 小 16 / 中 22 / 大 30 / 默认 中 |
| 外观 | 字体风格 | 现代圆体 / 经典衬线 / 默认 圆体 |
| 统计 | — | 只读 |

### FR-9 菜单栏（P0）
- 菜单栏常驻图标（SF Symbol「sparkles」模板样式），点击展开菜单（见信息架构）。
- 倒计时每分钟校准；运行状态点（绿/灰）。
- AC9.1：菜单实时显示倒计时。AC9.2：菜单栏应用不占 Dock，`⌘Q` 可用，退出菜单可达。

### FR-10 统计（P1）
- 今日点掉 N 个词、累计弹出 M / 点掉 K 个；近 7 日点掉柱状图（Swift Charts）。
- 今日数据按自然日 0 点重置，历史保留 30 天。
- AC10.1：点爆后统计立即 +1；跨天后图表正确分日。

### FR-11 视觉与动效（P0，审美红线）
- 遵循 `DESIGN.md`：毛玻璃词泡、柔和渐变描边、克制的阴影与动效；深浅壁纸下均可读。
- 词泡入场（0.45s spring，过冲 8%）、hover 释义（0.25s 延迟淡入）、点爆粒子（0.9s）、飘走（0.7s ease-out）。
- AC11.1：随机 5 张亮/暗壁纸下词泡对比度均 ≥ 4.5:1（带描边+阴影补偿）。AC11.2：动效时长可被系统「减弱动态效果」降级（后续版本；v0.1 至少不闪烁）。

### FR-12 多屏与全屏兼容（P1）
- 词泡可出现在所有屏幕（`NSScreen.screens`），自动布局。
- NSPanel 设置 `canJoinAllSpaces + fullScreenAuxiliary + nonactivating`：在「全屏 App」空间也可显示（按系统对辅助窗口的规则）。
- 屏幕空间变化（插拔显示器 / 切换分辨率）时，越界词泡自动吸附回可见区或飘走。
- AC12.1：双屏各屏按面积比例分配词数。AC12.2：拔掉副屏后其词泡 0.5s 内迁移或消失。

## 7. 非功能需求

| 类别 | 要求 |
|---|---|
| 性能 | 空闲 CPU < 1%；弹出一波（≤16 词泡）峰值 CPU 波动 < 5%；内存常驻 < 120MB |
| 电池 | 摆浮动效低帧率化；无波次时无定时高频唤醒（1s 倒计时可接受，或用分钟级校准） |
| 隐私 | 全本地运行；无网络权限申请；不上传词库/统计 |
| 权限 | 不申请「辅助功能/屏幕录制」；纯窗口方案实现穿透（逐词小窗 + 空区域自然穿透） |
| 兼容 | macOS 14.0+；适配浅色/深色菜单栏与系统字体；Intel 可编译（SwiftUI 通用） |
| 稳定性 | 词泡/释义卡窗口全部释放；异常退出不残留悬浮窗；设置损坏时回退默认值 |
| 无障碍 | 文本可被 VoiceOver 读到（词泡设 accessibilityLabel=word）；支持减弱动态效果原则 |

## 8. 边界情况与容错

1. **词本为空**：不发波；菜单栏提示「词库为空，请导入或切换词库」。
2. **重复导入**：自动去重，导入摘要报告「新增 X / 跳过 Y」。
3. **单词超长**（> 24 字符或含空格短语）：v0.1 完整显示但按长词测量宽度并限制每波数量，防止遮蔽过多屏幕。
4. **用户在打字时弹出**：词泡只占屏幕角落，不抢焦点，不拦截键盘——不打断输入。
5. **系统睡眠/锁屏**：唤醒后若错过波次，只补发一次，不积压补偿。
6. **多波残留**：默认先清理再弹出（见 FR-5），上限保护 24 词泡。
7. **释义缺失**（导入纯单词行）：释义卡显示「(暂无释义)」，仍可点爆与发音。
8. **菜单栏被占用/空间不足**：图标始终可渲染（系统保证菜单栏 Extra 区）。

## 9. 版本范围

**v0.1（本次交付）包含**：FR-1~FR-9、FR-11；FR-10 基础统计（今日/累计 + 7 日图）；FR-12 多屏与全屏兼容的基础实现。

**v0.1 不含**（列明避免误解）：
- 记忆曲线 / 遗忘算法与错题复现；
- 云同步、多设备；
- 在线词典补释义、分级词库商店；
- 快捷键全局热键、通知中心小组件；
- 「减弱动态效果」完整适配（仅保证无频闪）。

## 10. 路线图

- **v0.1**：本交付（核心闭环 + 设置 + 导入 + 统计 + 打包运行）。
- **v0.2**：间隔复现曲线（点爆后 1d/2d/4d 再弹）、「老是点不掉」的重词标记、发音快捷方式、暂停快捷键、更完整的减弱动效适配。
- **v1.0**：四六级/考研/雅思分级词库（内置可更新）、释义增强（词性/例句/音标）、导入支持 Anki/欧陆导出格式、自定义词泡内容（短语/公式）、小组件与提醒中心。

## 11. 成功指标（上线后跟踪）

- 日均点爆数 ≥ 30 / 人（说明暴露有效）；
- 词泡点爆率 ≥ 70%（留存时长内完成交互的比例）；
- 7 日留存 ≥ 40%；
- 每波平均耗时 ≤ 20s（不打扰原则）。

---

*PRD 结束。视觉与交互规范见 `DESIGN.md`。*
