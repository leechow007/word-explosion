# 词爆 WordPop — 视觉与交互设计规范（DESIGN.md）

> 本文档定义产品的**审美语言**，作为实现阶段的 UI 红线：
> 玻璃拟态、圆润亲和、克制渐变、弹簧动效、原生 mac 质感。
> 界面语言：中文为主（设置/菜单），单词内容为英文。

---

## 1. 品牌与气质

| 维度 | 定义 |
|---|---|
| 名字 | 词爆 WordPop ——「Pop」既是弹出，也是点爆 |
| 关键词 | 轻盈（lightweight）、梦幻（dreamy）、俏皮（playful）、原生（native） |
| 感觉 | 像在桌面上吹了一串肥皂泡，单词藏在泡里，双击「啵」地一声破掉 |
| 反关键词 | 厚重、警示感、弹窗恐惧、工具感报表 |

**一句话审美主张**：*让背单词成为桌面上最轻、最好看的打扰。*

## 2. 视觉支柱（三大原则）

1. **Glass on Glass（玻璃叠玻璃）**：词泡使用系统毛玻璃材质（`ultraThinMaterial`），让桌面壁纸透出来；对比度不足时用「渐变描边 + 彩色辉光阴影」补足，而不是压一层实色。
2. **One Accent（一个强调色）**：每颗词泡只允许一个低饱和强调色（描边/辉光同色），大面积永远是中性玻璃；颜色只承担「找到它」的功能。
3. **Physics, not Paths（物理动效）**：入场有弹簧过冲、点爆有粒子飞溅、飘走有浮力——所有动效模拟真实小物体，而不是 PowerPoint 淡入淡出。

## 3. 色彩系统

### 3.1 主题（词泡外观三档）

| 主题 | 名称 | 词泡填充 | 文字 | 描边/辉光 | 适用 |
|---|---|---|---|---|---|
| 🌅 晨雾 Glass | `glassLight` | `.ultraThinMaterial` | 深墨 `#1B1D29` | 强调色 45% 渐变 | 默认；亮/暗壁纸皆可读 |
| 🌌 午夜 Glass | `glassDark` | 深色玻璃（黑 38% + 白 6% 高光） | 近白 `#F5F6FA` | 白 20% + 强调色 | 深色壁纸/夜间 |
| 🌇 晚霞 Aurora | `aurora` | 强调色→粉紫渐变（87% 不透明） | 白 | 白 45% 内高光 | 想让词泡最显眼时 |

### 3.2 强调色盘（8 色，均可在设置中切换）

| 名称 | 色值 | 辉光用途 |
|---|---|---|
| Indigo 靛蓝 | `#5E5CE6` | 默认 |
| Blue 清蓝 | `#0A84FF` | |
| Cyan 青碧 | `#32ADE6` | |
| Mint 薄荷 | `#30D158` | |
| Teal 湖绿 | `#40C8BE` | |
| Pink 樱花粉 | `#FF6482` | |
| Purple 紫罗兰 | `#BF5AF2` | |
| Orange 落日橙 | `#FF9F0A` | |

规则：同一波词泡共享一个强调色；不同波次可在色盘中轮换，形成「每一波的颜色记忆」。

### 3.3 中性色与文字

- 主文字（玻璃主题上）：`#1B1D29`，不透明度 0.92。
- 释义卡文字：英文标题 `#1B1D29` / 中文释义 `#1B1D29` 65%。
- 界面背景：跟随系统 `windowBackground`；面板用 10pt 圆角、`secondarySystemFill` 弱分隔。
- 强调色用于「选中、主按钮、进行中状态」，不滥用。

## 4. 字体与字阶

| 用途 | 字体 | 规格 |
|---|---|---|
| 词泡单词（默认） | SF Pro Rounded Semibold | 小 16 / 中 22 / 大 30（设置可调） |
| 词泡单词（衬线选项） | New York Semibold | 同上（风格切换） |
| 释义卡 · 英文 | SF Pro Rounded Semibold | 20 |
| 释义卡 · 中文 | PingFang SC Medium | 15，行高 1.5，最多 3 行 |
| 菜单栏文案 | 系统默认 | — |
| 设置标题 | SF Pro / PingFang Semibold | 22 |
| 设置分组标题 | SF Pro / PingFang Semibold 13 + 大写间距 | secondary 色 |

## 5. 组件规范

### 5.1 词泡 Bubble（核心组件）

```
尺寸:    高 = 字号×1.5 + 28pt；宽 = 单词宽 + 44pt（≥ 56pt）
形状:    完整胶囊（Capsule），不使用直角
内边距:  水平 22 / 垂直 14（中号）
层级:    一个词 = 一个无阴影透明 NSPanel（热区即词泡本身）
描边:    1pt 渐变（强调色 45% → 同色 15%），外层 0.5pt 白 20%（玻璃主题）
阴影:    0, 8, 24, 黑 18%（玻璃）—— 由 SwiftUI 绘制，窗口本身 hasShadow=false
浮动:    正弦上下 ±2.5pt，周期 2.6–4.2s 随机错相（视觉呼吸，不抢注意力）
入场:    scale 0.62→1.0 + opacity 0→1，spring(0.42, 0.34)，过冲 ~8%，0.45s
```

**点击态**：单击轻轻压暗 6%；双击立即进入点爆，无选中框、无右键菜单。
**可读性兜底**：无论壁纸亮暗，玻璃词泡都带「白 20% 描边 + 黑 18% 阴影 + 强调色辉光」三层保障。

### 5.2 释义卡 Tip（hover 显示）

```
宽 ≤ 280pt（自动按释义长度，窄词可更小）
圆角 16pt；材质 ultraThinMaterial + 1pt 白 30% 描边 + 阴影(0,10,30,黑22%)
结构:
  ┌──────────────────────────────┐
  │  word            [发音按钮]   │  英文 Semibold 20 + speaker SF Symbol
  │  中文释义（最多 3 行，65% 黑） │
  └──────────────────────────────┘
出现: hover 0.25s 后，从词泡下方 8pt 淡入+上移 4pt；边缘自动翻转到上方/内收
消失: 移出词泡 0.15s 淡出；发音按钮点击时卡不消失
发音: 系统英文语音（默认 en-US），发音时按钮短暂变为波形图标
```

### 5.3 点爆粒子 Burst（双键确认的奖励）

双击触发，总时长 **0.9s**：
1. 0–0.12s：词泡放大到 1.14，同时白色光晕扩散；
2. 0.12s 起：14–22 颗粒子沿随机方向飞出（颜色=强调色 70% + 白 30%），半径 34–76pt，带 0.2 随机时延；
3. 粒子 ease-out 减速、透明度 1→0；文字反方向缩小至 0.6 并消失；
4. 0.9s 后窗口销毁；同一屏可同时爆多个（并发安全）。

粒子无残影、不使用截图预渲染；`Canvas + TimelineView(.animation)` 实现，CPU 友好。

### 5.4 菜单栏 Menu Bar

- 图标：SF Symbol「sparkles」，模板渲染，系统自动黑白适配。
- 菜单结构（中文，紧凑）：

```
词爆 WordPop                    ← 标题行（品牌色圆点 + 版本）
距下一波 12:34 · 运行中         ← 状态行（禁用态灰色）
──────────────────────────
立即弹一波
暂停弹出                       ← 运行/暂停切换文案
──────────────────────────
今日已点掉 6 个单词
──────────────────────────
设置…
退出词爆
```

### 5.5 设置窗口 Settings

```
窗口 760×620，无工具栏；圆角跟随系统；titlebar 透明化（traffic lights 悬浮）
布局: 左侧 200pt 图标导航（通用/词库/外观/统计/关于）
     右侧内容区（max 520pt 居中，ScrollView，无边框表单）
控件: 大号分段选择器（P0 项）+ 原生 Slider/Stepper + 中文标签右对齐说明
图标: SF Symbol 线性风格，选中态填充强调色
```

各页内容（含 16/20pt 大留白规范）：
- **通用**：弹出间隔预设胶囊组 + 自定义分钟；词数与留存时长两个 Slider（带实时数值与 emoji 化描述）；三个开关行。
- **词库**：来源三段式（内置基础/内置进阶/我的词本）；「导入 TXT/CSV」主按钮 + 格式提示折叠块；词条列表（可删）；规模徽章「126 词」。
- **外观**：三张主题卡（缩略图玻璃效果）横向排；8 色圆点强调色；字号与字体两行。
- **统计**：今日大数字 + 7 日柱状图（Swift Charts，强调色渐变柱）。
- **关于**：图标 + 名称 + 版本 + 一句话 slogan + 致谢。

## 6. 动效与反馈总表

| 动作 | 曲线 | 时长 | 细节 |
|---|---|---|---|
| 词泡入场 | spring(0.42, 0.34) | 0.45s | 缩放 0.62→1 + 淡入 |
| hover 释义出现 | easeOut | 0.18s | +4pt 上移、延迟 0.25s |
| 释义消失 | easeIn | 0.15s | — |
| 点爆 | 见 5.3 | 0.9s | 粒子 + 光晕 |
| 自动飘走 | easeInOut | 0.7s | 上移 46pt、旋转 ±2°、淡出 |
| 残留清理 | easeOut | 0.4s | 快速淡出 |
| 触感 | 原生 | — | 点爆时 haptic（可关） |

> 尊重「减弱动态效果」：所有动画均为非频闪、低幅度；v0.2 提供完全静态降级。

## 7. 交互规范

1. **永不抢焦点**：词泡/释义卡均为 `nonactivatingPanel`；双击点爆不激活应用、不打断正在输入的文字。
2. **空域穿透**：只有词泡窗口本体接收鼠标，其余屏幕区域鼠标事件直达下层 App。
3. **错误恢复**：点爆后 1.2s 内词泡必消失；飘走词泡不再响应点击。
4. **多屏**：词泡按屏幕可见面积分配；释义卡与词泡同屏显示，永不出屏。
5. **最小惊讶**：hover 只读信息、单击无破坏、双击才点爆；菜单项「立即弹一波」不重置周期。

## 8. 文案基调

- 中文界面用短句、轻语气：不写「您确认要删除吗」，写「清空我的词本」+ 副文案「共 N 词，删除后需重新导入」。
- 状态词统一：弹出 =「弹出一波」、点掉 =「点爆」、自动消失 =「飘走」、词库 =「词本」。
- 错误提示给「怎么办」而不是只给错误：如「没有读到有效单词，请检查每行格式：word 空格 中文」。

---
*设计规范结束。高保真交互稿见 `docs/mockup.html`（浏览器打开），最终以原生实现为准。*
